<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>com.zkhz</groupId>
        <artifactId>hcp-common</artifactId>
        <version>1.2.2</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>hcp-common-log</artifactId>

    <description>
        hcp-common-log日志记录
    </description>

    <dependencies>

        <!-- hcp Common Security -->
        <dependency>
            <groupId>com.zkhz</groupId>
            <artifactId>hcp-common-security</artifactId>
        </dependency>

        <!-- Servlet API（jakarta，SB3）：LogAspect 直接使用；spring-webmvc 传递的为 provided 不向下传递，故显式声明。运行期由 web 容器/spring-boot-starter-web 提供 -->
        <dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
            <scope>provided</scope>
        </dependency>

    </dependencies>
</project>
